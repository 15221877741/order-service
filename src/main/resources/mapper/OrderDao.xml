<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.orderservice.dao.OrderDao">

    <!-- 批量插入订单，回写自增 id -->
    <insert id="batchInsertOrders" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO t_order(order_no, user_id, total_amount, status, create_time, update_time)
        VALUES
        <foreach collection="list" item="order" separator=",">
            (#{order.orderNo}, #{order.userId}, #{order.totalAmount},
             #{order.status}, #{order.createTime}, #{order.updateTime})
        </foreach>
    </insert>

</mapper>
